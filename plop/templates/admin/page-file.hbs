import { Text } from "@julseb-lib/react"
import { AdminPage } from "components"

export const Admin{{>pascalName }}: FC = () => {
    return (
        <AdminPage title="{{ titleCase name }}">
            <Text tag="h1">{{ title }}</Text>
        </AdminPage>
    )
}
