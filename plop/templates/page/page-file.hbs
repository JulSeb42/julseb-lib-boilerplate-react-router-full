import { Text } from "@julseb-lib/react"
import { Page } from "components"

export const {{>pascalName }}: FC = () => {
    return (
        <Page title="{{ title }}">
			<Text tag="h1">{{ title }}</Text>
        </Page>
    )
}
